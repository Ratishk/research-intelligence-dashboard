fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-dotenv==1.0.1
apscheduler==3.11.0
feedparser==6.0.11
requests==2.32.3
beautifulsoup4==4.12.3
scrapling
anthropic==0.42.0
google-api-python-client==2.156.0
youtube-transcript-api==0.6.3
yfinance==0.2.51
praw==7.8.1
jinja2==3.1.5

# MCP stdio server (mcp_server.py) — proxies the dashboard API as LLM tools.
# Pulls in a newer starlette/pydantic than the FastAPI app pins, so install it
# into a SEPARATE virtualenv (see README-mcp.md), NOT the dashboard's runtime env.
mcp==1.27.2
